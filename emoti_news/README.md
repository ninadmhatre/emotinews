### Jobs

- Fetch the data from News API
  - Needs which contries, from which date-time
  - parse the data
    - send it to OPENAi (news title, description) -> title sentiment, news sentiment and is a clickbait?
    - compute uniq-id
    - store it in DB
  
